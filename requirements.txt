# Core dependencies
torch>=1.11.0
torchvision>=0.12.0
torchaudio>=0.11.0
omegaconf>=2.2.0
numpy>=1.21.0
tqdm>=4.64.0
editdistance>=0.6.0
opencv-python>=4.6.0
pillow>=9.0.0

# Optional dependencies for keypoint extraction
# pyopenpose  # For OpenPose keypoint extraction (install separately)
# mediapipe   # For MediaPipe keypoint extraction

# Optional dependencies
# pandas>=1.4.0  # Only needed for timestamp fallback